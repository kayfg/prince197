

public class Snippet {
	@Override 
		public void printInfo() // Circle
		{
			System.out.printf("Circle with an id of %d, radius of %.6f, area of %.6f, and perimeter of %.6f.", id, radius, calculateArea(), calculatePerimeter());
		}
}